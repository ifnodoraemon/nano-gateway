apiVersion: v1
kind: Service
metadata:
  name: {{ include "nano-gateway.fullname" . }}
  labels:
    {{- include "nano-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nano-gateway.selectorLabels" . | nindent 4 }}
